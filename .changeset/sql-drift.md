---
'firstly': patch
---

sqlAdmin: opt-in schema drift checks (`drift: { relationIndexes, primaryKeys, nullable, orphanColumns }`) with a `<SqlDrift />` UI to dry-run and apply, plus `sqlCreateIndex` / `collectEntities` helpers.
