---
'firstly': minor
---

Add the headless cell layer: `buildCells`, `displayCell`, `FF_Cell`, and the `FF_Config.cell` input registry - metadata-driven grids/forms with `%` widths and a per-cell escape hatch. The opinionated `FF_Grid` / `FF_Group` shells now ship as a copy-own `grid` boutique recipe (`src/boutique/grid`) instead of published components; `DemoGrid` / `DemoForm` are removed.
