---
'firstly': minor
---

sqlAdmin: opt-in `tokens` (bearer tokens to run SQL from a script or an AI; `read` = READ ONLY transaction over the extended protocol, acts as the minter, one path, logged) + `<SqlTokens />`; `console: false` to drop the console endpoint.
